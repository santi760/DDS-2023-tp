<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Monitoreo | Incidentes</title>
    <link rel="icon" href="../images/logo-login.png" type="image/x-icon" sizes="16x16 32x32">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
        }

        .container {
            text-align: center;
            margin: 50px auto;
            padding: 20px;
            max-width: 600px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        img {
            margin-bottom: 20px;
        }

        h1 {
            color: #333;
        }

        p {
            color: #666;
        }

        .volver-btn {
            text-decoration: none;
        }

        button {
            padding: 10px 20px;
            font-size: 16px;
            background-color: #3498db;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #2980b9;
        }
    </style>
</head>
<body>

<div class="container">
    <img src="{{#if state}}/images/sad.svg{{else}}/images/happy.svg{{/if}}" alt="{{#if state}}Cara feliz{{else}}Cara triste{{/if}}" width="100">
    <h1>No hay incidentes {{#if state}}abiertos{{else}}cerrados{{/if}} en esta comunidad</h1>
    <p>{{#if state}}Parece que no hay incidentes abiertos en esta comunidad en este momento.{{else}}Parece que no hay incidentes cerrados en esta comunidad.{{/if}}</p>
    <a href="/communities" class="volver-btn">
        <button>Volver</button>
    </a>
</div>

</body>
</html>
